name: Build and Deploy to Main Branch

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: Install Dependencies
        run: yarn install

      - name: Build
        run: yarn build

      - name: Deploy to Main Branch
        run: |
          git config --global user.email "stiant@getacademy.no"
          git config --global user.name "Stian Tharaldsen"
          git checkout main
          git add dist
          git commit -m "Deploy to main branch"
          git push origin main
